import { PaintCourseItemDirective } from './paint-course-item.directive';

describe('PaintCourseItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintCourseItemDirective();
    expect(directive).toBeTruthy();
  });
});
